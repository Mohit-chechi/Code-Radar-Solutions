#include <stdio.h>

int main() {
    char ch;
    // Take a character input from the user
    scanf("%c", &ch);
    // Print the ASCII value of the character
    printf("ASCII Value: %d\n", ch, ch);
    return 0;
}
